/* The workspace view-model. The generated views read every binding off this
   object; it is the prototype's renderVals() merged with the root props. Typed
   loosely on purpose: screens gain precise types as they are rewritten natively. */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Vals = Record<string, any>;
